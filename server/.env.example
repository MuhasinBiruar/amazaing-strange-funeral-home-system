APP_PORT=4000
CLIENT_URL=http://localhost:3000 # change in prod

# PostgreSQL connection (used by `pg`)
HOST=
PORT=
DATABASE=
DB_USER=
DB_PASSWORD=
DB_URL=postgresql://postgres.[DB_USER]:[DB_PASSWORD]@[HOST]:[PORT]/[DATABASE]

BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:4000 # change in prod
